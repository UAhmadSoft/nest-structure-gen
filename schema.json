{
  "root": "D:/projects/authar-quran-api",
  "paths": {
    "src": "/src",
    "entity_module": "/src/infrastructure/entities/db.ts",
    "repository_module": "/src/infrastructure/repository/repository.module.ts",
    "usecase_module": "/src/usecases/usecase.module.ts",
    "controller_module": "/src/infrastructure/controllers/controller.module.ts"
  },
  "char_primary_key": false,
  "insert_to_modules": true,
  "tables": [
    {
      "name": "EngagementSignals",
      "create_pagination_route": true,
      "create_relation_get_route": false,
      "properties": {
        "card_type": {
          "type": "enum",
          "enum": ["reflection", "challenge", "quiz"],
          "nullable": false,
          "dbEnumName": "engagement_signals_card_type_enum"
        },
        "card_id": {
          "type": "int4",
          "nullable": false,
          "comment": "ID of the reflection, challenge, or quiz"
        },
        "action_type": {
          "type": "enum",
          "enum": ["liked", "replied", "started", "completed", "attempted"],
          "nullable": false,
          "dbEnumName": "engagement_signals_action_type_enum"
        },
        "card_owner_id": {
          "type": "int4",
          "nullable": false,
          "comment": "User ID who created the card — denormalized for fast filtering"
        }
      },
      "relations": {
        "actor_id": {
          "type": "ManyToOne",
          "entity": "Users",
          "required": true,
          "onDelete": "CASCADE"
        }
      },
      "indexes": [
        {
          "columns": ["card_type", "card_id"],
          "name": "idx_engagement_signals_card"
        },
        {
          "columns": ["card_owner_id"],
          "name": "idx_engagement_signals_card_owner"
        },
        {
          "columns": ["card_type", "card_id", "actor_id", "action_type"],
          "unique": true,
          "name": "idx_engagement_signals_unique_action"
        }
      ],
      "constraints": []
    }
  ]
}
